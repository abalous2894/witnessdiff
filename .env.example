# Postgres connection for the API (docker-compose sets this automatically)
DATABASE_URL=postgresql://witnessdiff:witnessdiff@localhost:5433/witnessdiff

# Optional override when fixtures are mounted elsewhere
# WITNESSDIFF_FIXTURES_ROOT=/app/fixtures
